# build
Central storage for build tools

## Documentation

- [GitVersion versioning for .NET clients](docs/gitversion-versioning.md) — why
  the .NET clients are pinned to GitVersion 5.11.x, what GitVersion 6 breaks, and
  a clean reproduction on a test repo.
